extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 GERARDO KEYS PORTFOLIO 
        img(src="../static/images/logos/js.png").logo
        img(src="../static/images/logos/express.png").logo

      p.lead.text-light.
        My portfolio so far contains all of my projects that I have worked on since I've started learning up to my current projects. 
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    
      each val in projects
        .cell.tnails
          a(href= `/project/${val["id"]}`)
            img.thumbnail(src=`${val["image_urls"][0]}`)
            h5 #{val["project_name"]}

      
      
